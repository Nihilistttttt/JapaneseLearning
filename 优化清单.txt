LearnWord 项目优化清单
========================


================================================================================
一、Bug修复（不修就不能用 / 不修就出错）
================================================================================

1.1 严重Bug（运行时崩溃/数据错误）
────────────────────────────────────
1.  [✓] 修复 NestedScrollableHostBetween3Layers.findAllChildViewPagers() 返回值被丢弃 — 删除3个冗余方法，替换为canAnyChildScroll()，修复canDeepChildScroll NPE，移除调试日志
2.  [✓] 修复 ViewPager2Navigation 无限递归post — 改为事件驱动(pendingPosition+onMeaningVp2Ready回调)，重命名字段消除歧义(outer→overviewDetail, middle→meaning)，添加clearPendingNavigation防止过期意图，Fragment销毁时清理引用和pending
3.  [✓] 修复 LearnPageViewModel.observeForever 内存泄漏 — 改为同步查询(方案C)：Dao添加getWordByWordIdSync，Repository添加getWordByIdSync，checkAndNavigate改为databaseExecutor+postValue，消除observeForever和匿名Observer泄漏
4.  [✓] 单例VP2强引用内存泄漏修复 — 方案B(手动清理)：在Fragment.onDestroyView中主动将单例引用设为null，补全3个Fragment的遗漏清理点
5.  [✓] 修复Converter中"null"字符串反模式 — 回退核心改动，保留Repository空列表检查+Convert null安全两个防御性改进
6.  [✓] 实现数据导入管道，替换FirstPage中880行硬编码测试数据
7.  [ ] 修复 分词工具mecab.py 第155-158行 kana变量未赋值就使用的Bug
8.  [ ] 修复 匹配字典.py 第29行 for title, link in matches1 解包数量不匹配的Bug

1.2 逻辑Bug（功能异常/结果错误）
────────────────────────────────────
9.  [ ] 修复LearnPageAdapter.PageTransformer透明度计算（1-3*position导致滑动1/3就全透明）
10. [ ] 替换废弃API getUserVisibleHint()（LearnPageFragment第38行）
11. [ ] 词转化为字.py 返回值类型不一致问题（成功返回元组，失败返回字符串）
12. [ ] FirstPage修复intiViews拼写错误和按钮重复初始化

================================================================================
二、渲染性能（汉字-假名渲染管线优化）
================================================================================

2.1 热路径对象创建（每页面120+个Paint、60+次inflate、10+个WCV）
────────────────────────────────────
13. [✓] Paint对象复用 — SentenceView/WrappedPhraseView用成员变量kanjiPaint/kanaPaint(字体大小固定，构造时初始化)，BasicWordView用静态SparseArray<Paint>缓存(while循环中字体大小变化，key=textSizePx)
14. [✓] XML inflate替换为代码创建 — WordComponentView.initComponents中LayoutInflater.inflate(6个XML布局)改为直接new LinearLayout+new TextView×2，Select.layoutParams移除6个冗余字段
15. [ ] WrappedPhraseView逐字符创建完整WCV → 合并为单行WCV（当前每个字符创建一个WCV含3个View，10字符=30View；合并后1个WCV含12View，View数减半）

2.2 热路径微分配（单次开销小但调用频次高）
────────────────────────────────────
16. [✓] Rect在onTouchEvent中每次new 2个 → 提取为成员变量drawingBounds/clickableArea复用（WordComponentView.getClickableArea()）


2.3 渲染架构
────────────────────────────────────
22. ["✓] MainLearnPageFragment改为update模式 — 4个View(BasicWordView/SentenceView/MeaningView/IntegratedPartView)保留为成员变量，首次创建后addView，后续调update()刷新数据，IntegratedPartView复用TabLayout+VP2只更新adapter
23. [ ] 移除所有调试日志（Log.d遍布WordComponentView、NestedScrollableHost、BasicWordView、Constants等；calculateMargin中Log.d字符串拼接即使不输出仍求值）
24. [ ] 硬编码颜色/尺寸提取到资源文件（WordComponentView中setRadius(16)、setPadding(32,32,32,32)、parseColor("#2196F3")等）
25. [✓] 弹窗先构建后显示 — showInfoDialog等basicWord+meaning两个LiveData都回调后才popupWindow.show()，showSentencePopup等sentence回调后才show()，消除先弹空壳再填内容的闪烁

================================================================================
三、数据层（Room数据库 + 数据管道）
================================================================================

3.1 数据库配置与安全
────────────────────────────────────
26. [ ] Room改用WRITE_AHEAD_LOGING模式（当前TRUNCATE模式并发性能差）
27. [ ] Room实现正式Migration策略（当前fallbackToDestructiveMigration升级会丢数据）
28. [ ] WordDatabase.databaseExecutor改为private，提供execute方法封装（当前public暴露可被误调shutdown）
29. [ ] WordRepository.deleteAll()和批量插入添加事务支持（中途失败会导致数据不一致）
30. [ ] AndroidManifest.xml中allowBackup设为false（当前true，adb可提取数据库）
31. [ ] 数据库加密（SQLCipher），防止root设备直接读取

3.2 Repository/数据模型
────────────────────────────────────
32. [ ] WordRepository构造函数参数从Context改为Application（防止传入Activity Context）
33. [ ] JavaBean（Word/BasicWord等）添加equals/hashCode方法
34. [ ] 添加自定义Application类初始化全局单例（当前懒初始化导致首次使用延迟）

================================================================================
四、功能完善（用户体验直接相关）
================================================================================

35. [ ] 实现学习进度持久化（已学/未学/复习状态，不仅保存wordId）
36. [ ] 实现间隔重复算法（艾宾浩斯/SM-2复习调度）
37. [ ] 实现练习/测试模块（当前addPracticePage()空实现）
38. [ ] 完善搜索功能（SearchPageFragment功能不完整）
39. [ ] AudioManager添加音频焦点管理（与后台音乐冲突）
40. [ ] Activity添加screenOrientation配置（横竖屏切换状态恢复问题）

================================================================================
五、代码质量与架构
================================================================================

5.1 重复代码消除
────────────────────────────────────
41. [✓] 逐类提取基类消除重复代码 — JavaBean继承体系+WordView泛型基类+Fragment泛型基类+ConverterHelper，共消除约970行重复代码

5.2 API设计与可读性
────────────────────────────────────
42. [ ] WordComponentView三个构造函数改为工厂方法或Builder（当前通过参数数量区分，可读性差）
43. [ ] Constants中布局类型从int常量改为enum（SMALL/NORMAL/LARGE）
44. [ ] Select.layoutParams类名改为大写开头，selectLayout添加default分支
45. [✓] isNonClickableWordId()中String.valueOf(99)死代码删除，简化为wordId.equals("0")（SentenceView）

5.3 线程安全
────────────────────────────────────
46. [ ] AudioManager线程安全（pendingAudioUrl/currentAudioUrl/isPreparing无同步保护）

5.4 可测试性
────────────────────────────────────
47. [ ] 引入依赖注入框架（Hilt/Dagger），使ViewModel可单元测试
48. [ ] 添加Android单元测试和集成测试

================================================================================
六、构建与发布
================================================================================

49. [ ] 升级compileSdk/targetSdk从28到34+（Google Play要求≥33）
50. [ ] 升级Room从2.2.5到2.6+
51. [ ] 升级AppCompat/Material/ConstraintLayout/Gson等依赖到最新稳定版
52. [ ] 移除多余的multidex依赖（minSdk 28不需要）
53. [ ] 启用release构建ProGuard/R8混淆（当前minifyEnabled false）

================================================================================
七、Python数据处理脚本
================================================================================

7.1 工程规范
────────────────────────────────────
54. [ ] Python脚本提取公共模块（词转化为字/获取音便汉字/新时代获取音便字中重复的字典构建和回溯算法）
55. [ ] Python脚本统一路径配置（10个文件全部硬编码D:\Libraries\Documents\绝对路径）
56. [ ] Python脚本添加if __name__保护（8/10个文件导入即执行）
57. [ ] Python脚本添加requirements.txt
58. [ ] Python脚本添加logging模块替换print
59. [ ] Python脚本添加pytest单元测试
60. [ ] Python脚本添加输入验证和异常处理

7.2 算法性能
────────────────────────────────────
61. [ ] 回溯算法添加最大递归深度限制和预排序（防止指数级爆炸）
62. [ ] 获取音便汉字.py中copy.deepcopy改为手动浅拷贝（高频回溯中开销巨大）
